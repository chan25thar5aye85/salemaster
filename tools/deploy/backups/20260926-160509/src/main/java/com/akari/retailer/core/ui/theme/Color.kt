package com.akari.retailer.core.ui.theme

import androidx.compose.ui.graphics.Color

// ── Light theme ──
val Primary = Color(0xFF1976D2)
val Background = Color(0xFFE8F0FE)
val Surface = Color.White
val TextPrimary = Color(0xFF202020)

// ── Dark theme ──
val DarkPrimary = Color(0xFF64B5F6)       // lighter blue for dark backgrounds
val DarkBackground = Color(0xFF121212)    // standard Material dark surface
val DarkSurface = Color(0xFF1E1E1E)       // slightly elevated surface
val DarkOnPrimary = Color(0xFF003258)     // text on the lighter primary
val DarkTextPrimary = Color(0xFFE0E0E0)   // near-white text
